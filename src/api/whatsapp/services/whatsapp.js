'use strict';

/**
 * WhatsApp service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::whatsapp.whatsapp', {
  // This is a placeholder service for the Strapi API structure
  // The actual WhatsApp functionality is in src/services/whatsapp.js
});
